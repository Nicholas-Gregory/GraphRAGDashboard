{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"tsc --noEmit --watch --preserveWatchOutput --incremental\" \"esbuild ./src/main.tsx --bundle --outdir=dist --watch --servedir=dist\" \"esbuild --bundle ./src/style.css --outfile=./dist/style.css --watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "concurrently": "catalog:",
    "esbuild": "catalog:",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "tsx": "catalog:",
    "typescript": "catalog:"
  },
  "devDependencies": {
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3"
  }
}
